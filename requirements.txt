python-telegram-bot==20.7
python-dotenv==1.0.0
aiohttp==3.9.1
schedule==1.2.0
requests==2.31.0
tzlocal==5.2
pytz==2023.3
openai-whisper
flask
mutagen
flask-wtf
pyyaml
psutil

# IMPORTANT: PyTorch CPU-only variant to prevent OOM during docker build
# CPU wheel: ~130 MB (vs 2.3 GB with CUDA libraries)
# Whisper transcription still runs, falls back to CPU from CUDA if available
# To use CUDA/GPU: remove +cpu from torch wheels and install CUDA libraries
--find-links https://download.pytorch.org/whl/cpu/torch_stable.html
torch==2.3.0+cpu
torchaudio==2.3.0+cpu